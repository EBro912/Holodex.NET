namespace Holodex.NET.Enums
{
    public enum ChannelType
    {
        Subber,
        VTuber,
        /// <summary>
        /// Specifies both channel types. Used for function parameters.
        /// </summary>
        Both
    }
}
